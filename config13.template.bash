#! /bin/bash

# The linear contigs.
# - These contigs will not be rotated to a heuristically better origin.
# - The names used are the ones assigned by `normalize_assembly`. So,
#   this stage might have to be run twice.
# - Names are comma separated with no spaces.

LINEAR_CONTIGS=""
